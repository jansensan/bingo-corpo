{
  "name": "bingo-corpo",
  "description": "",
  "version": "0.0.1",
  "author": "Mat Janson Blanchet <mat@jansonblanchet.com> (https://mat.jansonblanchet.com/)",
  "license": "UNLICENSED",
  "scripts": {
    "convert:csv": "node modules/convert-csv.js",
    "build:styles": "gulp styles",
    "build:scripts": "rm -rf dist .parcel-cache && parcel build src/scripts/bingo.js --dist-dir public/scripts --no-cache",
    "build": "npm run build:styles && npm run build:scripts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "csv-parser": "^3.0.0",
    "parcel": "^2.12.0",
    "sass": "^1.58.3"
  }
}
